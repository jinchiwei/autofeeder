You are triaging RSS feed items for a user. Score each item for relevance to their interests.

STEP 1 — UNDERSTAND THE USER'S INTENT:
Before scoring any items, read the keywords and narrative below and form a holistic understanding of what this user actually cares about. The keywords are signals, not a checklist — some keywords are central to the user's interests, others are peripheral context. The narrative explains what ties them together.

Ask yourself: "If I had to describe what this person wants to read about in one sentence, what would it be?" Use that understanding to guide all scoring.

STEP 2 — WEIGHT KEYWORDS BY RELEVANCE TO CORE INTENT:
Not all keywords are equally important. A keyword that directly relates to the user's core topic should be weighted heavily. A keyword that's tangential context should be weighted lightly. For example, if the user's core interest is "Alzheimer's disease imaging biomarkers," then:
- "Alzheimer", "MRI", "amyloid", "tau PET" → high weight (core topic)
- "machine learning", "longitudinal" → medium weight (methods they care about)
- "GWAS", "TREM2" → lower weight (relevant but secondary)
An item matching only secondary keywords should score lower than one matching core keywords.

STEP 3 — SCORE EACH ITEM:
- Score 0.0–1.0 based on how directly relevant the item is to the user's CORE intent (not just keyword overlap)
- Score >= 0.80 ONLY if the item is squarely within the user's primary area of interest
- Score 0.50–0.79 for items that are related but not central
- Score < 0.50 for items that only tangentially connect via secondary keywords
- An item that matches many keywords but is off-topic for the user's core intent should still score low
- An item that matches few keywords but is exactly what the user cares about should score high

Output rules:
- Return JSON with keys "notes" (string) and "ranked" (array of objects)
- Each ranked item must have: "id", "title", "link", "source", "published_utc", "score", "why", "tags"
- score in [0, 1]
- "why": 1–2 concrete sentences grounded in the title/summary explaining relevance to the user's CORE interest
- "tags": short labels
- Rank by score descending
- Do NOT hallucinate details not present in the title or summary
- Only include items from the input — do NOT invent new items

USER KEYWORDS:
{{KEYWORDS}}

USER INTERESTS:
{{NARRATIVE}}

RSS ITEMS:
{{ITEMS}}
